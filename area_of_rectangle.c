#include <stdio.h>

int main()
{
    float l,b,area;
    printf("\n\nEnter the length and breath of Rectangle ");
    scanf("%f %f",&l,&b);
    area=l*b;
    printf("The Area of the Rectangle is %f\n\n",area);
    return 0;
}
